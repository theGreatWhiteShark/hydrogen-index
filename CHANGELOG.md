# Changelog

## Unreleased

- add the initial `hydrogen-index` CLI with `scan` and `validate` commands
- scan current and legacy Hydrogen songs, patterns, and drumkits into `index.json`
- validate generated indexes against a bundled JSON Schema derived from MADR 0002
